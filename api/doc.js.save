git add api/doc.js
git commit -m "fix(api): enforce allowed family_note languages via sanitizer"
vercel --prod --yes
 nano api/doc.js      # paste the file I gave you, save
npm i yaml           # if not already installed on this machine
git add api/doc.js package.json package-lock.json
git commit -m "feat(api): sanitize family_note languages + defaults"
vercel --prod --yesexport default async function handler(req, res) {
  const body = (req.method === 'POST' ? req.body : {}) || {};
  res.status(200).json({
    ok: true,
    kind: "doc",
    family_note: body.family_note || {}
  });
}
